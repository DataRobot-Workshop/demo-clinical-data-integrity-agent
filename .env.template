# Edited using `dr dotenv` on 2026-09-14T01:20:52Z
#
# The URL of your DataRobot instance API.
DATAROBOT_ENDPOINT="https://app.datarobot.com/api/v2"
#
# Refer to https://docs.datarobot.com/en/docs/api/api-quickstart/index.html#configure-your-environment for help.
DATAROBOT_API_TOKEN=
#
# Enter the local port to access the agent. Should be a unique value.
AGENT_PORT="8842"
#
# Enter the default execution environment for the agent's custom model.
DATAROBOT_DEFAULT_EXECUTION_ENVIRONMENT="[DataRobot] Python 3.11 GenAI Agents"
#
# Enter the default execution environment version ID for the agent's
# custom model.
# 
DATAROBOT_DEFAULT_EXECUTION_ENVIRONMENT_VERSION_ID="6a737b31b10b30076d534035"
#
# Enter a secret key used to sign session cookies. This should be a long, random string.
# The default value is randomly generated if the secret key is not already configured.
# 
SESSION_SECRET_KEY="VWd0SSIdZfFWZaDQNRgkjp14OQg9YbHp"
#
# Enter the URI of the database to use for the application.
# The default value is a SQLite database in the .data folder.
# 
DATABASE_URI="sqlite+aiosqlite:///.data/database.sqlite"
#
# Choose your backend OAuth provider.
# You can choose DataRobot (default), or you can choose Authlib and host it in the app for more customization.
# 
INFRA_ENABLE_OAUTH=""
#
# Enter the passphrase used to encrypt and decrypt the private key. This value is required if you're not using Pulumi Cloud.
# 
PULUMI_CONFIG_PASSPHRASE="swxLImEM6VsL3d6P8ljfY3GAHe5RG77o"
#
# Enter the default use case for this application. If not set, a new use case will be created automatically.
DATAROBOT_DEFAULT_USE_CASE="6aa73b07c7478ccb128c3a01"
#
# Set to 'true' to disable all OpenTelemetry tracing. Leave empty to keep tracing enabled.
OTEL_SDK_DISABLED=""
#
# Set to '1' to enable Python's UTF-8 mode. Needed on Windows, where the system locale encoding is not UTF-8.
PYTHONUTF8="1"
#
# Choose your LLM integration.
# 
# - For the simplest setup, select DataRobot's "LLM Gateway".
# - If you already have a custom model deployed LLM and a deployment ID, select "DataRobot Deployed LLM" (this sets USE_DATAROBOT_LLM_GATEWAY=0 automatically).
# - If you want to use your own LLM provider credentials (e.g. Azure OpenAI, AWS Bedrock,
# GCP VertexAI, Anthropic, Cohere, TogetherAI) wrapped in a governed deployment, select "LLM Blueprint with External LLM".
# - If you need full DataRobot governance and monitoring with LLM Blueprint support,
# select "LLM Blueprint with LLM Gateway" for the most production-ready option.
# 
INFRA_ENABLE_LLM="gateway_direct.py"
#
# Choose an LLM from the DataRobot LLM Gateway catalog to use for your LLM Blueprint.
LLM_DEFAULT_MODEL="datarobot/anthropic/claude-sonnet-4-6"
USE_DATAROBOT_LLM_GATEWAY="1"
#
# The local port to access the MCP Server. Should have a unique port.
MCP_SERVER_PORT="9000"
#
# Enter the default execution environment for the MCP server.
DATAROBOT_DEFAULT_MCP_EXECUTION_ENVIRONMENT="[DataRobot] Python 3.11 GenAI Agents"
#
# Enter the default execution environment version ID for the MCP server.
DATAROBOT_DEFAULT_MCP_EXECUTION_ENVIRONMENT_VERSION_ID="6a5a69d3cd08a31cc2fe625f"
#
# Select which tool integrations and features to enable (space to toggle, enter to confirm).
MCP_CLI_CONFIGS=""

# Clinical Data Integrity Agent — Snowflake connection
# Paste your Programmatic Access Token after SNOWFLAKE_PAT= (do not commit it).
# This PAT is all example data and is not valuable at all

# For local iteration without Snowflake, set USE_LOCAL_SQLITE=true and stage demo.db.
# USE_LOCAL_SQLITE=true

